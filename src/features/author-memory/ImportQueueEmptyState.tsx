import { EmptyState } from './components';

export function ImportQueueEmptyState() {
  return <EmptyState text="Р’ РѕС‡РµСЂРµРґРё РЅРµС‚ РєР°РЅРґРёРґР°С‚РѕРІ РїРѕРґ РІС‹Р±СЂР°РЅРЅС‹Рµ С„РёР»СЊС‚СЂС‹." />;
}
